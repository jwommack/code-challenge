import React from "react";
import {
    Typography,
} from "@material-ui/core";
const API = "http://localhost:3001";
export default () => (
    <Typography variant="display1">Not much of interest here, the meat is in Mock Data or {API}</Typography>
);